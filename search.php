<?php
    if (isset($_GET['query'])) {
        $query = $_GET['query'];
        // Виконайте операції пошуку тут, використовуючи значення $query.
        // Наприклад, виведіть результати пошуку або здійсніть інші дії.
        echo "Результати пошуку для: " . $query;
    } else {
        echo "Введіть запит для пошуку.";
    }
    ?>